Objective: @client/DBEDA.ipynb 를 정상적으로 실행시키는 것이 목표이다. @Screenshot.png 와 동일한 결과가 visualize되어야 한다.

다음의 요소들을 수행해야 한다.
- @README.md 를 바탕으로 docker 환경들을 세팅해라.
- @insert_dbsherlock_data.py, @devel/insert_json.py, @db/load_dbsherlock.py 를 참고하여 dbsherlock 데이터를 결정한다.
- anomaly transformer를 바탕으로 Anomaly Detection and Explanation 결과를 얻는다 (혹은 이미 폴더에 이를 수행한 결과가 있으면 그것을 load한다.).
- @client/DBEDA.ipynb 에서 connect_db가 잘 되는지 확인한다.
- @client/DBEDA.ipynb 에서 visualize가 제대로 동작하는지 확인한다.
- Visualize했을 때  @Screenshot.png 와 동일한 결과가 나오는지 확인해야 한다.

위를 수행하기 위한 상세한 계획을 작성하여 공유해라.